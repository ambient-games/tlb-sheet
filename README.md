This builds a character sheet for [The Last Book](http://thelastbook.us) for use in Roll20.net. It was built using [Svelte](http://svelte.dev).

Start a dev server using `yarn dev`. Build for using `yarn build`.
